 <h1 class="mt-4 text-muted font-weight-bold text-center"><u>Profile Page</u></h1>
 <div class="d-flex justify-content-center">
  <div class="d-inline rounded-circle p-4 my-4 display-2" style="color: rgb(186, 161, 174, .8); background-color: rgba(1, 5, 54, .8);"><%=@user.username%></div>
 </div>
  <div class="row mt-4">
    <div class="col-md">
      <div class="d-inline-flex p-2 rounded mb-2 bg-secondary text-light h5">User Info</div>
        <ul>
          <li>User Name: <strong><%=@user.username%></strong></li>
          <li>User Email: <strong><%=@user.email%></strong></li>
          <li>User Slug: <strong><%=@user.slug%></strong></li>
        </ul><br>

          <div class="d-inline-flex p-2 rounded mb-2 bg-primary text-light h5">Favorite Exercises</div>

          <h4>Add Another Workout</h4>
          <p>Please, click <a href="/workouts/new">here.</a></p><br>
    </div>

    <div class="col-md">
      <div class="d-inline-flex p-2 rounded mb-2 bg-light text-dark h5">Shared Workouts</div>
        <ol>
          <% @user.workouts.each do |workout|%>
            <li><a href="/workouts/<%= workout.id%>"><%=workout.created_at.in_time_zone("America/Los_Angeles").strftime("%A - %m/%d/%Y - %H:%M %p") %></a></li>
              <li><%=  %></li>
          <% end %>
        </ol><br>

        <%@user.exercises.each do |exercise|%>
          <h4> <%= exercise.name %> </h4>
          <% end %>
    </div>

    <div class="col-md">
      <div class="d-inline-flex p-2 rounded mb-2 bg-dark text-light h5">Private Workouts</div>
    </div>
  </div>
